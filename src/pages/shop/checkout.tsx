import { ExternalStoreRedirect } from "@/components/ExternalStoreRedirect";

export default function ShopCheckoutRedirectPage() {
  return <ExternalStoreRedirect title="Store" description="Checkout is handled in the external store." />;
}