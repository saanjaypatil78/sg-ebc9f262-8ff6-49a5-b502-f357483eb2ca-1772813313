import { ExternalStoreRedirect } from "@/components/ExternalStoreRedirect";

export default function ShopCartRedirectPage() {
  return <ExternalStoreRedirect title="Store" description="Cart is handled in the external store." />;
}