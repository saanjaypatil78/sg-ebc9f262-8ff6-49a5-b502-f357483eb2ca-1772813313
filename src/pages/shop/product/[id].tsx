import { ExternalStoreRedirect } from "@/components/ExternalStoreRedirect";

export default function ShopProductRedirectPage() {
  return <ExternalStoreRedirect title="Store" description="Product browsing is handled in the external store." />;
}